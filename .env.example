# API endpoint override (optional)
# For GitHub Pages + Cloudflare Worker proxy:
# VITE_API_URL=https://interviews.galiprandi.workers.dev
#
# For local dev using Vite proxy (default):
# VITE_API_URL=http://localhost:8010/api/graphql

# If unset, app uses:
# - localhost: /api/graphql (Vite dev proxy)
# - non-localhost: https://backoffice.rooftop.dev/api/graphql

VITE_API_URL=https://interviews.galiprandi.workers.dev

